#include<stdio.h>using namespace std;
int main()
{int t;
 scanf("%d",&t);
 while(t--)
 {int n;
 scanf("%d",&n);
 int a[n];
 int i;
 for(i=0;i<n;i++)
  scanf("%d",&arr[i]);
  int sum=0;int ans=INT_MIN;
  for(i=0;i<n;i++)
   {sum+=a[i];ans=max(ans,sum);
   if(sum<0)
     sum=0;}
     printf("%d",ans);}}
   
   
 
    
    
